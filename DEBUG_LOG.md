# DEBUG_LOG

本文记录第一关已经定位并修复的缺陷。行号以当前分支为准；Git commit 保留修改前实现，源码中的注释解释旧行为为什么错误。后续关卡继续在本文追加，不覆盖既有记录。

## D01：KB-001 清洗规则未真正执行

| 项 | 记录 |
|---|---|
| 现象 | 初始 rebuild 保留 18,628 行，报告中六类剔除数均为 0；原始数据明确包含坏日期、空金额、非正数量、坏外键和重复行。 |
| 假设 | 先怀疑数据集没有脏行；用失败样例逐类调用清洗函数后排除，发现 starter 只是复制原表。 |
| 验证 | commit `807253b` 新增 `test_clean_rows_normalizes_supported_fields` 等 4 个测试，修复前结果为 `4 failed`；修复后 rebuild 为原始 18,628、有效 18,290、剔除 338。 |
| 根因 | `starter/kbqa/cleaning.py:106` 的旧实现没有按 KB-001 顺序解析、校验和去重，也没有核验门店/商品外键。 |
| 修复 | `30453dc`：补齐日期规范化、金额/数量校验、ID 标准化、外键检查、精确重复删除及互斥清洗台账。 |
| 回归测试 | `starter/tests/test_cleaning.py:38`、`:59`、`:91`、`:105`；当前全部通过。 |

## D02：summary 使用错误的营业口径并漏掉结束日

| 项 | 记录 |
|---|---|
| 现象 | baseline 的 metrics 仅 1/6；月度净营业额、退款、订单、客单价和数量与独立 SQL 不一致。 |
| 假设 | 检查过金额单位和浮点精度；逐项对照 KB-001 后确认主要差异来自退款符号、订单定义和右开日期区间。 |
| 验证 | 用临时 SQLite 数据覆盖结束日销售、退款和重复订单行；旧实现会漏结束日，并把退款订单计入订单数。 |
| 根因 | `starter/kbqa/tools.py:52` 旧条件使用 `date < end`；旧聚合没有一致处理负金额退款、正向销售订单和净销量。 |
| 修复 | `13f79e8`：改为闭区间，净营业额纳入负数退款，退款额取绝对值，只统计正向销售订单，净销量扣退款。 |
| 回归测试 | `starter/tests/test_metrics.py:47`、`:65`；公开 metrics M01–M05 全部通过。 |

## D03：daily 与 summary 口径分叉且缺日期

| 项 | 记录 |
|---|---|
| 现象 | 相同筛选范围下，逐日净营业额求和不能稳定等于 summary；没有交易的日期直接消失，趋势图会断轴。 |
| 假设 | 曾怀疑是 JSON 小数精度；以整数分为单位核对后，确认是 daily SQL 和 summary SQL 条件不同且未补零。 |
| 验证 | 构造包含空白日、退款日和同订单多行的 3 天数据，逐日结果与汇总逐项比较。 |
| 根因 | `starter/kbqa/tools.py:133` 的旧 daily 聚合没有共用闭区间与正向订单口径，也没有生成完整日历。 |
| 修复 | `473c8c3`：daily 使用相同筛选和聚合规则，并为 `[start, end]` 每一天输出记录。 |
| 回归测试 | `starter/tests/test_metrics.py:77`，以及 `starter/tests/test_metric_consistency.py:20`、`:35`；公开 M06 通过。 |

## D04：health 和 data_quality 报告的是缓存/占位数据

| 项 | 记录 |
|---|---|
| 现象 | baseline health 0/1，文档数和有效明细不符合重建产物；HTML/非 UTF-8 文档会污染或跳过检索内容。 |
| 假设 | 先检查 API 序列化，再检查索引缓存；发现缓存键只依赖路径，内容变化后仍可能沿用旧索引。 |
| 验证 | 重建后直接核对 clean.db 行数、索引元数据和 API；最终 health 返回 35 篇文档、18,290 条有效明细。 |
| 根因 | `starter/kbqa/loader.py:211` 文档读取格式处理不完整；`starter/kbqa/index.py:189` 的缓存无法可靠感知内容变化；health 使用占位信息。 |
| 修复 | `6b506d0`：支持 md/txt/html、编码回退与 HTML 可见文本；内容哈希参与缓存键；rebuild 强制刷新；data_quality 增加互斥剔除总数。 |
| 回归测试 | `starter/tests/test_api.py:8`、`:68`；公开 health 1/1。 |

## D05：单接口正确不代表跨接口一致

| 项 | 记录 |
|---|---|
| 现象 | 固定样例测试无法防止未来修改造成 summary、daily 与清洗台账彼此漂移。 |
| 假设 | 单独断言几个常量覆盖不足，需要验证业务恒等式。 |
| 验证 | 对当前重建数据动态验证：原始行等于有效行加剔除行；全周期 summary 净营业额等于 daily 求和；任一单日 summary 等于对应 daily。 |
| 根因 | 测试套件缺少跨端点不变量，而不是某一条 SQL 的单点错误。 |
| 修复 | `ce52d6a`：新增三条动态一致性测试，避免将数据集数字写死。 |
| 回归测试 | `starter/tests/test_metric_consistency.py:12`、`:20`、`:35`。 |

## D06：趋势图在隐藏容器上初始化

| 项 | 记录 |
|---|---|
| 现象 | 页面功能正常，但浏览器控制台首次加载出现 ECharts 容器宽高为 0 的警告。 |
| 假设 | 初始 loading 状态通过 `v-show` 把容器设为 `display:none`，mounted 钩子仍立即初始化图表。 |
| 验证 | 浏览器重载捕获到警告；改为数据就绪后创建节点并等待一帧，再次重载没有新增告警。 |
| 根因 | `frontend/src/components/RevenueTrendChart.vue:21` 的旧初始化时机早于容器布局。 |
| 修复 | `986ceb3`：使用 `v-if` 生命周期、`nextTick + requestAnimationFrame` 和尺寸保护，并在首次有效初始化时注册 ResizeObserver。 |
| 回归测试 | `npm run build` 通过；浏览器实际验证全量、S01 和单日筛选，控制台无新增 error/warn。 |
